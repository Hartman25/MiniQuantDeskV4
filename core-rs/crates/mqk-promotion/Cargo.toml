[package]
name = "mqk-promotion"
version = "0.1.0"
edition = "2021"

[dependencies]
mqk-backtest = { path = "../mqk-backtest" }
mqk-portfolio = { path = "../mqk-portfolio" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
