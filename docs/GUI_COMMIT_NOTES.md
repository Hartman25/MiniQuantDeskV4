# Recommended commit sequence later

After the laptop-side repair pass, use commits like:

1. `gui: apply future bundle shell and truth model`
2. `gui: apply primary screen desk layouts`
3. `gui: apply navigation and layout convergence`
4. `gui: apply secondary and tertiary operator screens`
5. `gui: fix compile drift and finalize desk shell`

That gives you a clean audit trail instead of one giant mystery commit.
