Research Experiments

Structure (recommended)
- experiments/<name>/config.yaml
- experiments/<name>/run.py

Runs should write to: runs/<run_id>/...
and produce: manifest.json with input/output hashes (reuse existing manifest patterns).
